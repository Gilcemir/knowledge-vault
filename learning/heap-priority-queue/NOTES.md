# Working Notes — Heap / Priority Queue

## User preferences
- Conversation in **Portuguese (BR)**; all materials (lessons, reference, records) in **English**.
- **NEVER give solutions** to the 7 NeetCode problems. When stuck, give escalating hints: (1) restate/reframe the problem, (2) point to the pattern, (3) suggest the data-structure shape, (4) discuss a subproblem — never full code.
- Python only. ~30 min per session → one lesson OR one problem attempt per session.
- Baseline (2026-07-24): uses `heapq` push/pop mechanically; near zero on internals. Comfortable with earlier roadmap topics (arrays, binary search, trees, backtracking).

## Track plan

Lessons lead up to problems; problems are solved solo on LeetCode. Order within the problem set follows NeetCode's.

| # | Session | Type | Status |
|---|---------|------|--------|
| L1 | The array that thinks it's a tree (heap anatomy, index math) | lesson | ✅ delivered |
| L2 | Sift-down + build your own MinHeap (skeleton `lessons/0002-minheap-drill.py` + test harness) | lesson + coding drill | ✅ delivered |
| L3 | `heapq` fluency: min-only, negation trick, tuples, pushpop vs replace (drill `lessons/0003-heapq-drill.py`) | lesson + drill | ✅ delivered |
| P1 | Kth Largest Element in a Stream (LC 703, Easy) | problem | ✅ solved solo, no hints |
| P2 | Last Stone Weight (LC 1046, Easy) | problem | ✅ solved solo, no hints |
| L4 | ~~Top-K pattern~~ → **rescoped** (LR-0005) → delivered as *Choosing the key, choosing the direction* (drill `lessons/0004-topk-drill.py`) | lesson + drill | ✅ completed 2026-07-30 — 20/20 checks green, debrief done |
| P3 | K Closest Points to Origin (LC 973, Medium) | problem | ✅ solved solo, no hints — debriefed 2026-07-31 (LR-0009) |
| P4 | Kth Largest Element in an Array (LC 215, Medium) | problem | ✅ solved solo, no hints — `nlargest(k, nums)[-1]`. Debriefed 2026-07-31 (LR-0010); per-phase cost given, not earned |
| L5 | Greedy + heap: scheduling under an eligibility gate (`lessons/0005-greedy-and-heap-scheduling.html` + drill `lessons/0005-scheduling-drill.py`) | lesson + drill | ✅ completed 2026-08-05 (19/19), debrief done, LR-0011. Rewritten 2026-08-04 after Gil reported total loss (post-mortem below) |
| P5 | Task Scheduler (LC 621, Medium) | problem | ✅ solved solo, no hints — debriefed 2026-08-07 (LR-0012) |
| L6 | Composing heaps with other structures → delivered as *The heap of cursors: merging sorted sources* (`lessons/0006-the-heap-of-cursors.html` + drill `lessons/0006-merge-drill.py`) | lesson + drill | 🔵 delivered 2026-08-07 |
| P6 | Design Twitter (LC 355, Medium) | problem | ⬜ |
| L7 | The two-heaps pattern (balance invariant) | lesson | ⬜ |
| P7 | Find Median from Data Stream (LC 295, Hard) | problem | ⬜ |
| L8 | Capstone review: spaced retrieval quiz across everything | lesson | ⬜ |

Plan is adaptive — reorder/merge if Gil moves faster or slower than expected. Update Status column as sessions complete.

## Teaching notes
- **L6 design notes (2026-08-07)**. One skill: heap entries as CURSORS into data owned elsewhere (k-way merge of sorted sources). Vehicle: merged service-log timeline from a dict, deliberately not any of the 7 problems. Spoiler discipline for P6 (Design Twitter): the lesson teaches min-direction merge + early stop (`first_r`); absent on purpose — the words "twitter/tweet/feed/follow", any newest-first worked example (direction flip named in one sentence as L4's decision, left unworked), anything about MAINTAINING sources under mutation (adds/removes while queries arrive — the drill's closing message poses exactly that as the next problem). P6's real work left for him: recognizing the feed as a merge, flipping direction, walking cursors backwards from list ends, and the hashmap design itself. Concrete-first structure per the L5 post-mortem rules: §1 problem + worked output, §2 two working-but-wasteful baselines (re-sort O(N log N); scan-k-heads O(N·k)) before the fix, §3 full frontier trace table, records are DICTS to force the unique-comparable-prefix tuple (heapq docs' `(priority, count, task)` pattern, cited). Notation k/N/r defined in Part A with an explicit no-collision warning (LR-0009).
- **L6 drill answers, for the debrief**: `frontier` = "one head per unfinished source"; `heap_size` = "k"; concat_sort O(N log N), seed O(k), loop O(N log k), output O(N), `dominant` = "loop" (single winner again, unlike L5's tie); `first_r_cost` = O(k + r log k). Part C: C1 `["api-1","web-1","api-2"]`, C2 `["web-1","api-1","api-2","web-2"]`, C3 `["a-1","b-1","b-2"]`. Harness validated in scratchpad 2026-08-07: reference 22/22 green + six wrong variants each failing their targeted test (concat+sort → no-heap/no-resort; push-everything → frontier size check via instrumented max heap size; record-in-tuple → TypeError test; (t, idx, name) → name-outranks-position test; no-advance → small cases; full-merge-then-slice → lazy pop-count on 150k records). Laziness measured by op COUNTS, not wall-clock (per the 2026-07-31 finding that timing thresholds are dead on 3.14); `_run_counted` also tracks max heap size — reusable.
- **P5 debrief (2026-08-07, no hints)**. General heap+deque simulation, not the closed formula — the version that transfers to P6/P7. Highlights in LR-0012 (Q chosen right first try, pairing argument unprompted, log 26 collapse unprompted, sorted-by-construction earned in one round, zero defensive hedges). One slip, self-corrected in one round: phase 2 costed as T when the comprehension iterates m Counter values — same attribution family as LR-0006/0009; keep separating phases. Code review found no bugs.
- **Ship the empty skeleton with the standing demand.** "Manda a tabela de custos" drew a blank ("Que tabela? Não estou vendo nada") — he read "a tabela" as an artifact I'd sent, not one he fills. An empty 4-row skeleton naming his own code's phases fixed it instantly and the answers came back near-perfect. The demand is defined (LR-0010); the *delivery format* now is: always paste the empty table, rows = phases of *his* code.
- **Abstraction-level anxiety voiced at P5 hand-in** ("nível de abstração muito alto, não sei se estou no caminho certo") — despite solving it solo. Reassurance that landed: evidence (5/5 solo) + naming what composes (greedy + repeating gate + heap + clock, first time nothing works isolated) + "the difficulty is the curriculum working". Watch affect at L6, which is explicitly about composition — keep it concrete-first per the L5 post-mortem rules, and consider opening L6 by *naming* that composition is the skill now.
- **L5 v1 failed — post-mortem (2026-08-04)**. Gil read it three times and could not state even the objective. Root cause: abstraction-first ordering — v1 opened with greedy proof theory (coin change, exchange argument) before any concrete problem; the drill's actual scenario appeared only in §3, in two abstract prose sentences; and the lesson contained **zero hand-traces**, for a student whose strongest channel is tracing on paper. An earlier fix (2026-08-03, "state the problem first" — see global memory) patched the coin example's missing setup but not the structure. Rewrite: §1 states the drill scenario with a worked timeline on a concrete input, §2 shows the single-heap failure on the same input, §3 traces the two-container loop as a table (now/waiting/heap/move/output), greedy + exchange argument demoted to §6 *after* the mechanics, framed as "what you just did has a name". **Rules earned: (1) a lesson's §1 must state the concrete problem its drill solves — abstractions enter only as the fix to a failure the student just watched; (2) every new loop/pattern gets a full state-table trace on a small input; (3) one lesson = one new skill — L5 v1 promised three "walk-away" items and delivered none.** The trace example `[(2,4,"x"),(0,1,"y"),(3,8,"z")]` was chosen to differ from Part C's inputs (C1–C3 answers stay unspoiled). Drill unchanged except a §-number reference; drill answers in the note above still hold.
- Gil learns well by tracing on paper and asks cost questions unprompted — lean into hand-traces + complexity reasoning. He got sift-up right cold on the first try (LR-0002).
- Drills should ship with a runnable test harness (immediate automatic feedback). Validate the harness against a reference impl **in the scratchpad**, never in the workspace.
- Verified 2026-07-24: textbook sift-down pop vs `heapq.heappop` — popped value and multiset always match, array arrangement differs in ~10% of random cases, both always valid heaps. Don't let him think that's a bug.
- Quizzes: keep all options the same word count (skill rule).
- After each solved problem, debrief: complexity achieved, pattern named, add learning record if insight is non-obvious.
- Encourage predicting `heapq` internal array states before running code — cheap desirable difficulty. L3's drill formalises this as a `PREDICTIONS` dict checked by the harness; reuse the pattern.
- L3 deliberately teaches `heappushpop`/`heapreplace` as *API mechanics only*, with no top-k framing — the size-k-heap pattern is L4's job and would spoil P1 (LC 703). Keep it that way if L3 is ever revised.
- **Correction (2026-07-28)**: `python3` on PATH is 3.9.6, but **`python3.14` (3.14.6) is installed** at `/opt/homebrew/bin/python3.14` and is what drills should run under (L3's `__pycache__` is already cpython-314). `X | Y` unions and 3.14 max-heap functions *are* available there. Supersedes the 2026-07-27 note that said otherwise. Drill run lines should say `python3.14`.
- **Harness gotcha, cost me a debug cycle**: since Python 3.13 the compiler **dedents docstrings**, so `src.replace(fn.__doc__, "")` silently strips nothing and every banned token named in the docstring gets flagged in the student's code. L4's `_source_of` now cuts the first triple-quoted block textually instead — copy that version into future drills. (L3 is unaffected only by luck: its docstrings never name a banned token.)
- **Quiz answers leaked position**: L1–L3 used `data-answer="0"` for *every* quiz — correct option always first. L4 varies it. Keep varying it.
- Validate every drill harness in the scratchpad against a reference solution **plus deliberately wrong variants**, not just a correct one. L4's harness was proved to catch: whole-tuple comparison on ties, collect-then-trim (unbounded), unguarded `heapreplace`, and a `sorted()` one-liner.
- L4 can skip the "why not just sort" motivation — Gil raised the `heapify` vs push-loop build-cost question himself at the end of L3 (LR-0004). Open L4 directly at the size-k heap, and fold in `nlargest`/`nsmallest` (the last unmet `heapq` success criterion).
- **L8 must re-test cold**: `heappushpop` vs `heapreplace` — Gil named the right one on P1 but skipped deriving why `heapreplace` breaks a size-k heap, so it was given, not earned (LR-0005). Fluency only; no evidence of storage strength.
- **Ask for complexity per phase, then which dominates** — never one aggregate number (LR-0006). On P2 the aggregate was right while the underlying attribution was wrong (`heapify` blamed for the log factor), and separating the phases is the only thing that surfaces it.
- **L5 design notes (2026-07-31)**. Spoiler discipline, same as L3→P1: the lesson teaches the **one-way** gate (arrival — once arrived, stays arrived) and states in one sentence that a *repeating* gate reuses the identical containers and invariant, without working it. P5 (Task Scheduler) therefore still requires the adaptation one-way → repeating, which is the real work. Deliberately absent: the words "task scheduler", "idle slots", and anything resembling the `(max_freq-1)*(n+1)` formula. The drill's vehicle (`processing_order`, arrival-gated single worker) is close to LC 1834, which is not in this track's 7 problems.
- **L5 drill answers, for the debrief**: `shrinking_quantity` = "orders not yet processed"; `iterations_jump` = n; `iterations_tick` = T; prepare/loop/output = O(n log n)/O(n log n)/O(n); `dominant` = **"tie"** — deliberately the first cost table in this workspace with no single winner. Part C: C1 `["c","a","b"]`, C2 `["a","b","c"]`, C3 `["a","c","b"]`.
- ~~Still to add at the L5 debrief~~ **Done 2026-08-05**: cheat sheet gained "Greedy + heap: the eligibility gate" and "Bounding a data-dependent loop: the shrinking quantity" (incl. the "Q shrinks; progress grows" mirror-image gotcha from LR-0011). Patterns table row L5 marked ✅.
- **L5 drill debrief (2026-08-05, 19/19)**. Two bugs, both fixed from one hint round each, answers never given: (1) heap tuple `(-priority, name)` — two fields for a three-level tie rule; Pylance's unused-`arr` warning was the tell (see LR-0011's note: unused-variable warnings on unpacked domain data are policy bugs); (2) `shrinking_quantity` = "ticks elapsed" — monotonicity inverted, fixed by the all-arrive-at-tick-0 discriminating scenario (LR-0011). Everything else in Part A was right first pass, including `dominant: "tie"` — the LR-0010 per-phase format transferred on a scaffolded surface. Minor: he reported "tudo verde" once while the buffer was unsaved and disk still failed — verify by running, not by report.
- **P5 handover (2026-08-05), what was and was not given**: given — LC 621 by name, the fact that its gate is *repeating* (the lesson's §3 and the harness's closing message already say this), the one-sentence adaptation ("act parks the item back with its next eligibility time instead of discarding it"), and a pointer to the new cheat-sheet sections. NOT given — which container the repeating gate's waiting area should be, any cooldown/idle formula, whether the answer needs the full schedule or just its length (that reading is his job). Standing demand repeated: per-phase costs **by name, boring phases included**, before any code review. Crux question posed: "what is the item's next eligibility time, and what data must travel with it when it is parked?"
- **Harness finding worth reusing — wall-clock thresholds are dead on 3.14 (2026-07-31)**: the tick-by-tick variant ran **50 million loop iterations in 1.31s**, so the intended "this cannot finish in time" test passed it. Replaced with `_with_time_limit()` in the L5 drill: SIGALRM + `setitimer` raising an `AssertionError` carrying the teaching message, arrivals pushed to 2e9, fallback to measuring elapsed where SIGALRM is missing. **Copy that helper into any future drill that needs to reject an algorithm on cost rather than on output.** Calibrate against 3.14, never against intuition about Python speed.
- **L5 harness validated 2026-07-31** against a reference impl (19/19) plus five deliberately wrong variants: tick-by-tick clock, `(-priority, name, arrival)` tuple order, no-heap `max()` scan, release-only-when-empty, and no-sort-by-arrival. First pass caught two of them *only* via the 300-case stress test, whose message dumps 30 tuples — bad teaching surface per the rule below. Added two targeted cases (arrival-outranks-name needs a third order so both candidates are already waiting; unsorted input needs an early arrival listed last) so every variant now fails a small case with a readable message first.
- **Teach the shrinking-quantity argument explicitly in L5** — bounding a loop whose iteration count depends on the data. P2 needed it (2 popped / ≤1 returned ⇒ ≤ n−1 iterations); Gil didn't attempt it. P5 (Task Scheduler) needs it again. Don't assume it transferred.
- **Revised 2026-07-31 by LR-0010 — read this before the item below.** The "per-phase cost" demand was asked on P1–P4 and logged four times as a *habit* gap. On P4 he said plainly he did not understand the question: "custa quanto" and "custa quanto por fase" were the same question to him. Part of the gap was **vocabulary, not motivation**. What worked: a worked cost table on content he had already mastered (heapify-all + k pops from L4), labelled explicitly as *the format, not the content*, plus the why (an aggregate hides attribution errors and hides that dominance shifts with the parameters). **Rules earned: define a demand before repeating it a third time; one open question per debrief round, never four stacked; when he says "just give me the answer", give it — the Socratic budget for that point is spent, re-test cold at L8 instead.**
- **Never assume Python built-ins are known (2026-07-31)**: `zip` was unknown, which made the `nlargest` source unreadable and the seed-cost question unanswerable. He asked for it unpacked into primitives — right call. Fix that worked: *run* it, don't describe it (`zip` stops at the shorter side → exactly `k` pairs; `it` keeps its position, so the following `for elem in it` resumes at `k+1`, which is why the sweep is `n − k`). Same family as the 2026-07-29 materials gap. Scratchpad bonus: unpacking that line as `for elem in it: if i >= k: break` yields an identical list but consumes `k+1` elements — a silent off-by-one that explains why `heapq` uses `zip`. Reusable demo material for L8.
- **Recurring habit gap**: Gil does not volunteer complexity when handing in a solution — asked for it explicitly in the P1, P2 and P3 briefings, skipped all three times, and answers correctly only when pressed. Not a knowledge gap (P1 answers were right, cold). Keep demanding it *before* the code review, every problem, until it's automatic — it's the first interview follow-up question. **Sharper variant seen on P3**: even when asked for three phases by number, only the middle (the "interesting" one) comes back — the O(n) build and the O(k) output are treated as not counting. Ask for the boring phases by name.
- **Never write `n` for the count of `nlargest`/`nsmallest`** — always the caller's `k`, plus an explicit "heapq spells this parameter `n`" warning. On P3 the crossed `n`s made Gil state the cost exactly inverted (`k log n`), and he held it through a round of prose (LR-0009). **Audit done 2026-07-31**: L3 §5, L4 §5 and the cheat-sheet contract section now write the count as `k`, reserve `n` for `len(iterable)`, and each carries a "The `n` that is not your `n`" gotcha callout. L4 §5 and the cheat sheet also derive `n log k` from the `zip(range(k), it)` seed + `for elem in it` pass, per the source-before-prose rule below. The one remaining bare `n` is the verbatim docs quote in L4 §6, now annotated as such on purpose.
- **For stdlib cost questions, show `inspect.getsource` before writing prose** (2026-07-31). Two Socratic rounds and a pointer to the cheat-sheet cost table failed to dislodge a wrong `nsmallest` cost; one look at `zip(range(n), it)` fixed it in a single round and he generalised to `nlargest` unprompted. Generalises beyond `heapq`.
- **Graduated to LR-0008 (2026-07-30)**: the hedging-against-his-own-invariants pattern recurred a third time (`next(counter)` to "skip the zero"). See `learning-records/0008-hedges-against-invariants-he-established.md` — when he hands in code, ask him to name the invariant a defensive line protects against instead of just deleting it.
- **Done 2026-07-30**: the withheld Part A cost table is now in `reference/heap-cheat-sheet.html` ("Three strategies, costed per phase"), added at the debrief as planned. The sign-of-the-tiebreaker table went into the same file under "Tie order".
- **L4's designed payoff — delivered at the 2026-07-30 debrief**: he got all 11 cost cells right first pass, including `heapify_all` being O(n) and therefore the time winner over the bounded heap for small k. He did *not* conclude "so the bounded heap is pointless" — but he also did not raise the tension himself; the harness's closing question prompted it. Framing that landed: "ganhar no eixo errado não é ganhar" (memory + streaming are different axes, `heapify_all` doesn't get slower under a memory constraint, it stops being available). Re-test cold at L8.
- **P4 briefing given 2026-07-31** (for the debrief record — what was and was not handed to him): no pattern name, no structure name, no strategy recommendation. Two decisions asked of him — (a) what changes when the input is a finished array instead of a stream, (b) what "kth largest" means with duplicates present — plus the standing demand for per-phase costs *by name, boring phases included*, before any code review. Quickselect was named only as an optional stretch **after** a heap solution, never as the target. LC 215's own follow-up ("without sorting") was not quoted, to avoid ruling strategies out for him.
- Pattern for problem handovers that worked well on P1: no pattern name, no structure name — just "what does the shape of the API tell you about where cost can live" plus one crux question. He needed zero hints. Keep briefings that thin.
- Watch for Python-level slips distinct from topic misconceptions — L3's only bug was storing an `itertools.count()` object instead of `next(counter)`. Naming the symptom ("this output is alphabetical") was enough; no explanation needed. **Second instance (L4 Part C, 2026-07-30)**: he filled `PREDICTIONS` with *string literals of* the lists (`"[('d', 9), ...]"`) instead of the lists. Same family — writing something that renders like the value instead of the value. It cost two rounds because the harness prints with `str()`, so `you predicted [('d', 9)…], heapq produced [('d', 9)…]` looked identical; he reasonably concluded the harness was broken. **Two fixes for future drills: use `{got!r}` in every assertion message, and assert the type before the value.** He waved off the distinction as unimportant ("tanto faz") — worth one calibration pass later, since str-vs-list is the exact class of bug that eats a debug cycle.
- Harness authoring rule earned the hard way (2026-07-30): a drill's assertion messages are teaching surface, not diagnostics. Any message that can print two different objects identically will be read as a harness bug — and the student will stop trusting the feedback loop, which is the whole mechanism.
- **Materials gap Gil reported (2026-07-29)**: `nlargest`/`nsmallest` were *used* heavily in L4 (§1 table, §4 docs quote, §5 ties, Part C) on the strength of two lines in L3 §5 that gave only the signature and O(n log k). He could not tell whether they operate on a heap or on a plain list, what they return, or whether they are Python-specific — and §5's tie discussion was therefore unreadable. Fixed by adding L4 §5 "`nlargest`/`nsmallest`, defined" (contract table, "they build a heap, they do not consume one", the three cost paths incl. the `n==1` and `n>=len` shortcuts, cross-language note), a contract paragraph in L3 §5, and a cheat-sheet subsection. **Lesson for future sessions: when a lesson leans on a stdlib function for a whole section, state its input/output/mutation contract explicitly before reasoning about its behaviour.** A signature plus a complexity is not an introduction.
- Verified 2026-07-27: L3 drill scenarios A/B — pushing `[18, 23, 3, 19, 2]` one at a time gives `[2, 3, 18, 23, 19]`, while `heapify` on the same list gives `[2, 18, 3, 19, 23]`. Both valid; textbook sift-down agrees with CPython on both. Roughly 47% of random 5–7 element lists show this divergence.
